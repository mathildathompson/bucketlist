<% @destinations.each do |d| %>

<div class="destinations">
	<h1><%= d.name %></h1>
</div>

<div class = "todoitem">

<% d.to_do_items.each do |to_do_item| %>
    <h4><%= render to_do_item %></h4>
</div>
	

    <% end %>

     <div class = "addtodoitem">
    <%= render :partial => 'to_do_items/form', :locals => {:destination_id => d.id} %>
    </div>

<% end %>